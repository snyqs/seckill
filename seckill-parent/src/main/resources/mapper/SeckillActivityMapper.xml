<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seckill.mapper.SeckillActivityMapper">

    <select id="selectActivityPage" resultType="com.seckill.entity.SeckillActivity">
        SELECT a.*
        FROM t_seckill_activity a
        WHERE a.deleted = 0
        <if test="status != null">
            AND a.status = #{status}
        </if>
        ORDER BY a.create_time DESC
    </select>

    <!-- Query active activities -->
    <select id="selectActiveActivities" resultType="com.seckill.entity.SeckillActivity">
        SELECT a.*
        FROM t_seckill_activity a
        WHERE a.deleted = 0
        AND a.status IN (0, 1)
        AND a.end_time > NOW()
        ORDER BY a.start_time ASC
    </select>

    <!-- Update activity status to in progress -->
    <update id="updateActivityStatusToInProgress">
        UPDATE t_seckill_activity
        SET status = 1, update_time = NOW()
        WHERE status = 0
        AND start_time &lt;= NOW()    -- 30行
        AND end_time > NOW()
        AND deleted = 0
    </update>

    <!-- Update activity status to ended -->
    <update id="updateActivityStatusToEnded">
        UPDATE t_seckill_activity
        SET status = 2, update_time = NOW()
        WHERE status = 1
        AND end_time &lt;NOW()
        AND deleted = 0
    </update>

</mapper>