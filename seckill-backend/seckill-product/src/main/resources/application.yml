server:
  port: 8082
  servlet:
    context-path: /api

spring:
  # ?????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/seckill_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456

  # Redis??????
  redis:
    host: localhost
    port: 6379
    password:
    database: 0

  # ?????ant-path-matcher???SpringFox/Knife4j
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

# MyBatisPlus??
mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.seckill.product.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# JWT??
jwt:
  secret: seckill-system-2025-secret-key-123456
  expire: 7200000

# Knife4j??????????
knife4j:
  enable: true
  setting:
    language: zh-CN
  openapi:
    title: ????-????????
    version: 1.0.0
    description: ?????????
    base-package: com.seckill.product.controller
    path-selector: /**
